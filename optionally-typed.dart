// A function without types.
recalcuate(origin, offset, estimate) {
    // ...
}

// A function with types (and an optional parameter).
num recalculate(Point origin, num offset, {bool estimate: false}) {
    // ...
}
